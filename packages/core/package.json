{
  "name": "@splash/core",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "node -e \"import('fs').then(fs=>fs.rmSync('dist',{recursive:true,force:true}))\""
  },
  "dependencies": {
    "@splash/utils": "workspace:*",
    "@splash/config": "workspace:*",
    "@splash/safety": "workspace:*",
    "@splash/memory": "workspace:*",
    "@splash/providers": "workspace:*",
    "@splash/connectors": "workspace:*",
    "@splash/skills": "workspace:*",
    "@splash/tools": "workspace:*",
    "@splash/plugins": "workspace:*",
    "ink": "^5.0.1",
    "react": "^18.3.1",
    "zod": "^3.23.0"
  }
}
